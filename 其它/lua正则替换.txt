
LUA源代码-宏展开版格式优化:

使用工具notepad、正则表达式、VS快捷键ctrl+a、ctrl+k、ctrl+f

1、{{}} 这种转换成 
   {
        {

        }
    }

第一步：({)(?![\s\t]*$)    替换成     \1\r\n

第二步：(\S+.*)(\})(.*\S+.*)$    替换成     \1\r\n\2\3

第二步：(\S+.*)(;)(.*\S+.*)$    替换成     \1\2\r\n\3


替换完后编译有少许报错，手动改好即可
